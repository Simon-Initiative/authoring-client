name: CI

on:
  push:
    branches:
      - master
      - 'SPRINT-*'
  pull_request:
    branches:
      - '*'

jobs:
  ci:
    runs-on: ubuntu-latest
    container:
      image: node:12

    steps:
      - uses: actions/checkout@v1
      - name: Install & Tests
        run: |
          yarn
          npm run build
