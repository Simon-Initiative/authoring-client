import * as React from 'react';

import './markers.scss';

export const PronunciationEnd = (props) => {
  return (
    <span className="PronunciationSentinel">
      pronunciation end
    </span>);
};
