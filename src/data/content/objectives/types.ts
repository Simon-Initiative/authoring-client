export enum KnowledgeCategory {
  DomainSpecific = 'domain_specific',
  DomainGeneral = 'domain_general',
}

export enum LearningProcess {
  FluencyBuilding = 'fluency_building',
  Refinement = 'refinement',
  SenseMaking = 'sense_making',
}

