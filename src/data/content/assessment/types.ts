import { FLOW_ELEMENTS, BODY_ELEMENTS } from 'data/content/common/elements';

export const ALT_FLOW_ELEMENTS = ['alternatives', ...FLOW_ELEMENTS];
export const QUESTION_BODY_ELEMENTS = ['alternatvies', 'custom', ...BODY_ELEMENTS];

